/*
 * Validation Routines
 */

/*
 * PO must be 5 digits
 */
function checkPO () {
  
}

/*
 * PAJ code must be PAJ/PJJ followed by 5 digits
 */
function checkPAJ () {
  
}

/*
 * Full Box Weight must be number with 2 decimal places
 * Might allow lbs/oz at some point
 */
function checkFullBoxWeight () {
  
}

/*
 * Empty Box Weight must be number with 2 decimal places
 * Might allow lbs/oz at some point
 */
function checkEmptyBoxWeight () {
  
}

/*
 * productCount must be integer >0
 */
function checkProductCount () {
  
}

/*
 * Product Weight must be number with 2 decimal places
 * Might allow lbs/oz at some point
 */
function checkProductCount () {

}

function nextOp1 () {
  localStorage.setItem('VTBCountVerification', JSON.stringify(formObject));
}

/*
 * Other Event Handlers
 */

/*
 * Mainline code
 */

// declare variables
var formObject; {
  "purchaseOrder", "";
  "pajCode", "";
  "fullBoxWeight", 0;
  "emptyBoxWeight", 0;
  "productCount", 0;
}
// initialize validation routines


// 

